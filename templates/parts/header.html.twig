

<nav class="navbar navbar-expand-lg bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Nft MarketPlace Admin</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class=" menu m-3 text-white" href="{{ path('app_admin') }}">Admin</a>
                </li>
                <li class="nav-item">
                    <a class=" menu m-3 text-white" href="{{ path('app_category_index') }}">Categories</a>
                </li>
                <li class="nav-item">
                    <a class=" menu m-3 text-white" href="{{ path('app_gallery_index') }}">Galleries</a>
                </li>
                <li class="nav-item">
                    <a class=" menu  m-3 text-white" href="{{ path('app_nft_index') }}">Nfts</a>
                </li>
                <li class="nav-item">
                    <a class=" menu  m-3 text-white" href="{{ path('app_user_index') }}">Utilisateurs</a>
                </li>
                {% if app.user%}
                <li class="nav-item">
                    <a class=" menu  m-3 text-white" href="{{ path('app_logout') }}">Déconnection</a>
                </li>
                {%  endif %}
            </ul>
        </div>
    </div>
</nav>